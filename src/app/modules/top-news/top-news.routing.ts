import { Route } from '@angular/router';
import { TopNewsComponent } from './top-news.component';

export const topNewsRoutes: Route[] = [
    {
        path     : '',
        component: TopNewsComponent
    }
];
